<footer class="doc-footer">
    <div>
        <strong>{{company.name}}</strong> — {{company.vat}} — {{company.email}} — {{company.phone}}
    </div>
    <div class="muted">{{company.address}}</div>
</footer>